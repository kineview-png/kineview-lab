# React 19 hace que varias librerías nativas declaren peers incompatibles.
# El flag va acá para que TODO npm install del proyecto lo herede, no solo el
# que uno recuerda escribir a mano.
legacy-peer-deps=true
